# 安全说明

## 报告漏洞

请不要通过公开 issue 报告安全问题。请使用本仓库的 **Security → Report a vulnerability**（私密漏洞报告）
描述问题与复现步骤，收到后会尽快处理。

---

## 相对上游修复的问题

### 推送接口未做身份校验（高危）

**影响版本**：上游 StarBot 3.0 全部版本

**问题**：`starbot-onebot-adapter` 在 StarBot 的服务端口（默认 7827）上注册了推送接口
（默认路径 `/onebot/send`），该接口直接把请求体转发给 OneBot 实现，**全程没有任何身份校验**。
配置项中的 `one-bot-http-token` 只用于 StarBot 出站调用 OneBot 时的认证，与入站请求无关。

**后果**：任何能访问到该端口的人都可以借由 StarBot 向任意 QQ 群或用户发送任意消息，包括 @全体成员。
由于官方部署文档推荐将 StarBot 部署在云服务器上，若未额外配置防火墙，该端口可能直接暴露在公网。

**修复**：核心中其实已经预留了这一机制——`com.starlwr.bot.core.model.Sender` 的 `token` 字段注释明确写着
「在推送平台接口处验证 Token」，且 `StarBotMessageSender` 发送时已经会带上 `Authorization: Bearer <token>`
请求头，只是接收端的校验从未实现。本仓库补全了校验端，并额外增加了来源 IP 白名单与频率限制。

### 登录凭据明文落盘（中危）

**问题**：哔哩哔哩登录凭据（`SESSDATA`、`bili_jct`、`buvid3`）以明文写入工作目录下的 `cookies.json`，
文件权限为默认权限。

**后果**：这组凭据等同于账号的完整控制权，可用于发布内容、关注、消费。任何能读到该文件的本机进程、
误提交到仓库的备份、或打包发送的日志目录都会导致账号失窃。

**修复**：默认以 AES-256-GCM 加密存储，密钥独立成文件，凭据文件与密钥文件均以 0600 权限创建。
检测到旧的明文文件会自动迁移并备份。

### 来源 IP 判定可被 DNS 阻塞（中危，本仓库新增代码中发现）

**问题**：IP 白名单若使用 `InetAddress.getByName` 解析待检地址，传入主机名时会触发 DNS 解析。

**后果**：开启 `trust-proxy` 后，攻击者可在 `X-Forwarded-For` 中填入解析缓慢的域名，
使每个请求都在过滤器上阻塞一次 DNS 查询，构成拒绝服务面。

**修复**：只接受 IP 字面量，主机名一律拒绝。

### 可绕过 QQ 的 @全体成员 权限（中危，本仓库新增功能中发现）

**发现于 4.1.0-beta1，4.1.0-beta2 修复。**

2026-08-05 实测：一个在群里只是普通成员、客户端里根本点不出 @全体成员 的账号，
**经 OneBot 接口发送 `[CQ:at,qq=all]` 却能真的 @ 到全体**，并照常扣减每日额度——
QQ 服务端这一路没有做客户端那道权限检查。

这不是本项目引入的缺陷，但**本项目此前会无条件把使用者配置的 `{at=all}` 发出去**，
等于替使用者利用了这个漏洞。能做不等于该做：绕过权限强行 @ 全体属于滥用，
有招致账号风控的风险，而风控的代价是**整条推送链路停摆**。

修复方式是主动补上客户端那道检查：机器人不是群主或管理员时把 `@全体成员` 摘掉，
正文照常送达。判据用的是群内角色而非 `get_group_at_all_remain` 的 `can_at_all`——
后者实测在无权限的群里同样返回 `true`，它报的是额度不是权限。

同时加了每日限次，两个维度独立记：账号 10 次/天（全群共享）、单群 20 次/天。
**用 4.1.0-beta1 的请务必升级。**

### 直播间数据包解析缺少边界校验（中危）

**问题**：上游解析长连接数据包时不校验长度字段，也不限制解压后的体积。

**后果**：长度字段异常时解析循环不会推进，导致死循环占满 CPU；恶意构造的压缩包可耗尽内存。

**修复**：长度字段非法时终止解析，解压产物超过 32 MB 放弃，递归展开限制在 3 层内。

---

## 部署建议

### 必做

**不要把服务端口暴露到公网。** 默认配置只监听 `127.0.0.1`：

```yaml
server:
  address: 127.0.0.1
```

若确需从其他机器调用推送接口，请同时做到三点：配置强随机的 `api-token`、把来源地址加入
`allow-ips` 白名单、在防火墙上限制来源。

```yaml
starbot:
  adapter:
    onebot:
      security:
        allow-ips:
          - 127.0.0.1/32
          - 10.0.0.0/8
        fail-on-weak-config: true   # 外网可达的部署建议开启
      senders:
        - name: qq-onebot
          api-token: <openssl rand -base64 32 生成的随机串>
```

**使用小号登录哔哩哔哩。** 程序默认开启 `auto-follow`，会主动用登录账号执行关注操作。
凭据一旦泄露，攻击者即取得该账号的完整控制权。

**配置界面若要开到公网，必须启用口令登录。** 放开 `allow-ips` 而不设口令，
等于把「改推送目标、看运行数据、读群成员绑定关系」的权限对全网敞开——
启动日志会为这种组合打出 ERROR，但配置本身不会被拒绝。

```yaml
starbot:
  core:
    config-ui:
      allow-ips:
        - 0.0.0.0/0               # 交给反向代理与 https 去挡
      auth:
        password: <可直接填明文，启动时会给出可替换的哈希串>
        totp-secret: <Base32 密钥，留空则不启用二次验证>
```

开启后访问令牌不再是凭据，改为登录换会话。三点须知：

- **口令与密钥都以明文或可离线爆破的形式存在配置文件里**，务必把
  `application.yml` 的权限收紧到 `600`
- 必须走 https。会话 Cookie 的 `Secure` 标记跟随当前连接判断，
  部署在反向代理之后时需要设置 `server.forward-headers-strategy`，
  否则程序看到的永远是明文连接，Cookie 不会带上 `Secure`
- 怀疑 Cookie 泄漏时用界面上的「退出全部设备」，不必重启进程

### 建议

- 以独立的低权限用户运行，或放入容器
- 定期检查 `cookies.json` 与 `cookies.key` 的权限仍为 `600`
- 打包日志或工作目录发给他人前，先移除 `cookies.json`、`cookies.key` 与 `datasource.json`；
  4.1.0 起还要移除 **`state.json`**，它含群成员的 QQ 号与其绑定的哔哩哔哩 uid
- 关闭不需要的调试日志：`live-room-raw-message-log` 与 `dynamic-raw-message-log` 会把原始消息完整写入日志

### 关于观众数据

4.1.0 起会按用户记录直播间内的互动量（弹幕条数、礼物价值等），用于排行榜与个人数据查询，
并缓存观众头像地址。这些都是**直播间里本就公开可见**的信息，不涉及私聊内容或好友关系。

两点仍值得注意：

- **绑定关系无法验证归属**。没有接口能证明某个 QQ 号与某个哔哩哔哩账号属于同一人，
  绑定只核对昵称防打错。因此控制台**只提供解绑、不提供代人绑定**——
  替别人建立一条对应关系，等于凭空造出一份看似可信的证据
- **`state.json` 里的绑定关系是可以反推出人的**。换机器、发日志、交接部署时按上面那条处理
- **金额是另一回事**。直播收益、每位观众送了多少钱，属于主播的经营信息与观众的消费记录，
  与「弹幕多热闹」不是同一类。4.2.0 起「谁能看到金额」是会话自己的属性，
  **默认私聊显示、群聊隐藏**，且只能在控制台改——若允许群里的人自己把它打开，这道设置就形同虚设

### 关于反向代理

若在 Nginx 之后部署，需显式开启 `trust-proxy`，否则来源 IP 始终是代理的地址：

```yaml
starbot:
  adapter:
    onebot:
      security:
        trust-proxy: true
```

**只有在请求确实全部经过可信代理时才可开启。** 直接暴露的服务开启此项等同于让 IP 白名单失效——
任何人都能伪造 `X-Forwarded-For`。

---

## 已知风险（非本项目可修复）

- **NapCat 等 OneBot 实现属于非官方 QQ 协议实现**，使用存在账号被封禁的风险
- **QQ 登录态可能在无预兆的情况下失效，需要重新扫码**。NapCat 的快速登录依赖 QQ
  保存的登录态，实测并不总是可靠：服务端或 NapCat 重启后，有一定概率提示登录态已失效。
  根因是腾讯对非官方协议实现的风控，NapCat 侧无法根治
  （上游 [#1632](https://github.com/NapNeko/NapCatQQ/issues/1632)、
  [#1728](https://github.com/NapNeko/NapCatQQ/issues/1728) 均已关闭，
  给出的是更新 GUID、降级版本一类的绕行建议而非修复），本项目侧同样无法代为解决。
  **本项目能做的是不让它静默发生**：OneBot 连接中断会触发健康告警，
  配置 Webhook 告警通道后可推送到手机——注意 QQ 告警此时已随之失效，
  必须配置不经过机器人链路的通道，详见配置模板中 `alert` 段的说明
- **哔哩哔哩的接口风控**可能导致直播间长连接被限制，此时弹幕、礼物等事件无法接收；
  开播下播推送可由备用直播推送兜底
